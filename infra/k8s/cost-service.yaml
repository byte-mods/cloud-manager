apiVersion: apps/v1
kind: Deployment
metadata:
  name: cost-service
  namespace: cloud-manager
  labels:
    app: cost-service
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cost-service
  template:
    metadata:
      labels:
        app: cost-service
        component: backend
    spec:
      containers:
        - name: cost-service
          image: cloud-manager/cost-service:latest
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef:
                name: cloud-manager-config
            - secretRef:
                name: cloud-manager-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8085
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8085
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: cost-service
  namespace: cloud-manager
spec:
  selector:
    app: cost-service
  ports:
    - port: 8085
      targetPort: 8085
  type: ClusterIP

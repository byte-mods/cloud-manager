apiVersion: apps/v1
kind: Deployment
metadata:
  name: tutorial-service
  namespace: cloud-manager
  labels:
    app: tutorial-service
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tutorial-service
  template:
    metadata:
      labels:
        app: tutorial-service
        component: backend
    spec:
      containers:
        - name: tutorial-service
          image: cloud-manager/tutorial-service:latest
          ports:
            - containerPort: 8089
          envFrom:
            - configMapRef:
                name: cloud-manager-config
            - secretRef:
                name: cloud-manager-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8089
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8089
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: tutorial-service
  namespace: cloud-manager
spec:
  selector:
    app: tutorial-service
  ports:
    - port: 8089
      targetPort: 8089
  type: ClusterIP
